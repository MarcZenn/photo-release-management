# Copy to .env.local and fill in real values. Never commit .env.local.

# Local dev stack (from `supabase start` output) or the linked project's
# API settings (Project Settings > API in the Supabase dashboard).
SUPABASE_URL=
SUPABASE_ANON_KEY=

# Server-side only. Bypasses RLS entirely — never expose to the frontend
# bundle or commit to version control.
SUPABASE_SERVICE_ROLE_KEY=

# Frontend (Vite). Must use the VITE_ prefix to be exposed to the client
# bundle — same URL/anon key as above, just re-declared for the SPA build.
# Also set these in the Vercel project's environment variables.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
